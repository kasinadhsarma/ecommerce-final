# Lib Directory Documentation

This document provides detailed information about the main source code in the `lib/` directory of the e-commerce application.

## Directory Structure

The `lib/` directory contains the main Dart code for the application, organized in a feature-based structure:

```
lib/
├── firebase_options.dart
├── main.dart
├── models/
│   ├── cart_model.dart
│   ├── loyalty_model.dart
│   ├── order_model.dart
│   ├── product_model.dart
│   └── user_model.dart
├── providers/
│   ├── auth_provider.dart
│   ├── cart_provider.dart
│   ├── loyalty_provider.dart
│   ├── loyalty_provider_new.dart
│   ├── order_provider.dart
│   └── product_provider.dart
├── screens/
│   ├── home_screen.dart
│   ├── splash_screen.dart
│   ├── auth/
│   │   ├── forgot_password_screen.dart
│   │   ├── login_screen.dart
│   │   └── register_screen.dart
│   ├── cart/
│   │   ├── cart_screen.dart
│   │   ├── checkout_screen.dart
│   │   └── order_success_screen.dart
│   ├── product/
│   │   ├── category_products_screen.dart
│   │   ├── product_details_screen.dart
│   │   └── search_screen.dart
│   └── profile/
│       ├── edit_profile_screen.dart
│       ├── edit_profile_screen_fixed.dart
│       ├── loyalty_rewards_screen.dart
│       ├── order_details_screen.dart
│       ├── order_history_screen.dart
│       ├── profile_screen.dart
│       └── store_details_screen.dart
├── services/
│   ├── api_service.dart
│   ├── auth_service.dart
│   ├── biometric_auth_service.dart
│   ├── notification_service.dart
│   └── stripe_web_service.dart
├── utils/
│   ├── app_utils.dart
│   ├── firebase_config.dart
│   ├── js_interop_utils.dart
│   └── web_compatibility_helper.dart
└── widgets/
    └── common_widgets.dart
```

## Main Application Files

### main.dart

The entry point of the application that sets up the widget tree, initializes services, and configures platform-specific settings.

Key features:
- Provider setup for state management
- Firebase initialization with platform-specific configurations
- Theme configuration
- Navigation setup

```dart
void main() async {
  WidgetsFlutterBinding.ensureInitialized();

  // Initialize Firebase with platform check
  try {
    if (kIsWeb) {
      // Web-specific initialization
      await Firebase.initializeApp(options: DefaultFirebaseOptions.web);
      StripeWebService.initStripeWeb();
      debugPrint('Running in web mode');
    } else {
      // Mobile initialization
      await Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform);
      await SystemChrome.setPreferredOrientations([
        DeviceOrientation.portraitUp,
        DeviceOrientation.portraitDown,
      ]);
    }
  } catch (e) {
    debugPrint('Firebase initialization error: $e');
  }

  runApp(
    MultiProvider(
      providers: [
        ChangeNotifierProvider(create: (_) => AuthProvider()),
        ChangeNotifierProvider(create: (_) => ProductProvider()),
        ChangeNotifierProvider(create: (_) => CartProvider()),
        ChangeNotifierProvider(create: (_) => OrderProvider()),
        ChangeNotifierProvider(create: (_) => LoyaltyProvider()),
      ],
      child: const MyApp(),
    ),
  );
}

class MyApp extends StatelessWidget {
  const MyApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'E-Commerce App',
      theme: ThemeData(
        primarySwatch: Colors.blue,
        visualDensity: VisualDensity.adaptivePlatformDensity,
      ),
      home: const SplashScreen(),
      // Route definitions
    );
  }
}
```

### firebase_options.dart

Firebase configuration options generated by the FlutterFire CLI. Contains platform-specific Firebase configurations for Android, iOS, macOS, and web.

## Models

The models directory contains data model classes that define the structure of the application data.

### product_model.dart

Defines the Product class that represents product information:

```dart
class Product {
  final int id;
  final String title;
  final double price;
  final String description;
  final String category;
  final String image;
  final Rating rating;

  Product({
    required this.id,
    required this.title,
    required this.price,
    required this.description,
    required this.category,
    required this.image,
    required this.rating,
  });

  factory Product.fromJson(Map<String, dynamic> json) {
    return Product(
      id: json['id'],
      title: json['title'],
      price: json['price'].toDouble(),
      description: json['description'],
      category: json['category'],
      image: json['image'],
      rating: Rating.fromJson(json['rating']),
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'id': id,
      'title': title,
      'price': price,
      'description': description,
      'category': category,
      'image': image,
      'rating': rating.toJson(),
    };
  }
}

class Rating {
  final double rate;
  final int count;

  Rating({required this.rate, required this.count});

  factory Rating.fromJson(Map<String, dynamic> json) {
    return Rating(
      rate: json['rate'].toDouble(),
      count: json['count'],
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'rate': rate,
      'count': count,
    };
  }
}
```

### user_model.dart

Defines the User class that represents user information:

```dart
class User {
  final String id;
  final String email;
  final String displayName;
  final String? photoUrl;
  final String? phoneNumber;
  final String? address;
  final DateTime createdAt;
  final bool isVerified;

  User({
    required this.id,
    required this.email,
    required this.displayName,
    this.photoUrl,
    this.phoneNumber,
    this.address,
    required this.createdAt,
    required this.isVerified,
  });

  // Factory constructor to create User from Firebase User
  factory User.fromFirebase(firebase_auth.User firebaseUser) {
    return User(
      id: firebaseUser.uid,
      email: firebaseUser.email ?? '',
      displayName: firebaseUser.displayName ?? '',
      photoUrl: firebaseUser.photoURL,
      phoneNumber: firebaseUser.phoneNumber,
      createdAt: firebaseUser.metadata.creationTime ?? DateTime.now(),
      isVerified: firebaseUser.emailVerified,
    );
  }

  // Factory constructor from JSON
  factory User.fromJson(Map<String, dynamic> json) {
    return User(
      id: json['id'],
      email: json['email'],
      displayName: json['displayName'],
      photoUrl: json['photoUrl'],
      phoneNumber: json['phoneNumber'],
      address: json['address'],
      createdAt: DateTime.parse(json['createdAt']),
      isVerified: json['isVerified'],
    );
  }

  // Convert to JSON
  Map<String, dynamic> toJson() {
    return {
      'id': id,
      'email': email,
      'displayName': displayName,
      'photoUrl': photoUrl,
      'phoneNumber': phoneNumber,
      'address': address,
      'createdAt': createdAt.toIso8601String(),
      'isVerified': isVerified,
    };
  }

  // Create a copy with updated fields
  User copyWith({
    String? displayName,
    String? photoUrl,
    String? phoneNumber,
    String? address,
    bool? isVerified,
  }) {
    return User(
      id: id,
      email: email,
      displayName: displayName ?? this.displayName,
      photoUrl: photoUrl ?? this.photoUrl,
      phoneNumber: phoneNumber ?? this.phoneNumber,
      address: address ?? this.address,
      createdAt: createdAt,
      isVerified: isVerified ?? this.isVerified,
    );
  }
}
```

### cart_model.dart

Defines the CartItem class for shopping cart items and related functionality:

```dart
class CartItem {
  final int productId;
  final String title;
  final double price;
  final String image;
  int quantity;

  CartItem({
    required this.productId,
    required this.title,
    required this.price,
    required this.image,
    this.quantity = 1,
  });

  double get totalPrice => price * quantity;

  // Factory constructor from JSON
  factory CartItem.fromJson(Map<String, dynamic> json) {
    return CartItem(
      productId: json['productId'],
      title: json['title'],
      price: json['price'],
      image: json['image'],
      quantity: json['quantity'],
    );
  }

  // Convert to JSON
  Map<String, dynamic> toJson() {
    return {
      'productId': productId,
      'title': title,
      'price': price,
      'image': image,
      'quantity': quantity,
    };
  }

  // Create a copy with updated fields
  CartItem copyWith({
    int? quantity,
  }) {
    return CartItem(
      productId: productId,
      title: title,
      price: price,
      image: image,
      quantity: quantity ?? this.quantity,
    );
  }
}
```

### order_model.dart

Defines the Order class for tracking purchase history:

```dart
class Order {
  final String id;
  final String userId;
  final List<CartItem> items;
  final double totalAmount;
  final DateTime dateTime;
  final String status;
  final String? trackingNumber;
  final String? shippingAddress;
  final String paymentMethod;

  Order({
    required this.id,
    required this.userId,
    required this.items,
    required this.totalAmount,
    required this.dateTime,
    required this.status,
    this.trackingNumber,
    this.shippingAddress,
    required this.paymentMethod,
  });

  // Factory constructor from JSON
  factory Order.fromJson(Map<String, dynamic> json) {
    return Order(
      id: json['id'],
      userId: json['userId'],
      items: (json['items'] as List)
          .map((item) => CartItem.fromJson(item))
          .toList(),
      totalAmount: json['totalAmount'],
      dateTime: DateTime.parse(json['dateTime']),
      status: json['status'],
      trackingNumber: json['trackingNumber'],
      shippingAddress: json['shippingAddress'],
      paymentMethod: json['paymentMethod'],
    );
  }

  // Convert to JSON
  Map<String, dynamic> toJson() {
    return {
      'id': id,
      'userId': userId,
      'items': items.map((item) => item.toJson()).toList(),
      'totalAmount': totalAmount,
      'dateTime': dateTime.toIso8601String(),
      'status': status,
      'trackingNumber': trackingNumber,
      'shippingAddress': shippingAddress,
      'paymentMethod': paymentMethod,
    };
  }
}
```

### loyalty_model.dart

Defines the LoyaltyPoints and Reward classes for the loyalty rewards program:

```dart
class LoyaltyPoints {
  final String userId;
  final int points;
  final DateTime updatedAt;
  final List<PointsTransaction> transactions;

  LoyaltyPoints({
    required this.userId,
    required this.points,
    required this.updatedAt,
    required this.transactions,
  });

  // Factory constructor from JSON
  factory LoyaltyPoints.fromJson(Map<String, dynamic> json) {
    return LoyaltyPoints(
      userId: json['userId'],
      points: json['points'],
      updatedAt: DateTime.parse(json['updatedAt']),
      transactions: (json['transactions'] as List)
          .map((item) => PointsTransaction.fromJson(item))
          .toList(),
    );
  }

  // Convert to JSON
  Map<String, dynamic> toJson() {
    return {
      'userId': userId,
      'points': points,
      'updatedAt': updatedAt.toIso8601String(),
      'transactions': transactions.map((t) => t.toJson()).toList(),
    };
  }
}

class PointsTransaction {
  final String id;
  final int points;
  final String type; // 'earned' or 'redeemed'
  final String description;
  final DateTime dateTime;

  PointsTransaction({
    required this.id,
    required this.points,
    required this.type,
    required this.description,
    required this.dateTime,
  });

  // Factory constructor from JSON
  factory PointsTransaction.fromJson(Map<String, dynamic> json) {
    return PointsTransaction(
      id: json['id'],
      points: json['points'],
      type: json['type'],
      description: json['description'],
      dateTime: DateTime.parse(json['dateTime']),
    );
  }

  // Convert to JSON
  Map<String, dynamic> toJson() {
    return {
      'id': id,
      'points': points,
      'type': type,
      'description': description,
      'dateTime': dateTime.toIso8601String(),
    };
  }
}

class Reward {
  final String id;
  final String title;
  final String description;
  final int pointsCost;
  final String? image;
  final DateTime expiryDate;
  final bool isActive;

  Reward({
    required this.id,
    required this.title,
    required this.description,
    required this.pointsCost,
    this.image,
    required this.expiryDate,
    required this.isActive,
  });

  // Factory constructor from JSON
  factory Reward.fromJson(Map<String, dynamic> json) {
    return Reward(
      id: json['id'],
      title: json['title'],
      description: json['description'],
      pointsCost: json['pointsCost'],
      image: json['image'],
      expiryDate: DateTime.parse(json['expiryDate']),
      isActive: json['isActive'],
    );
  }

  // Convert to JSON
  Map<String, dynamic> toJson() {
    return {
      'id': id,
      'title': title,
      'description': description,
      'pointsCost': pointsCost,
      'image': image,
      'expiryDate': expiryDate.toIso8601String(),
      'isActive': isActive,
    };
  }
}
```

## Providers

The providers directory contains state management classes using the Provider pattern.

### auth_provider.dart

Manages user authentication state:

```dart
class AuthProvider with ChangeNotifier {
  User? _user;
  bool _isLoading = false;
  String? _error;

  User? get user => _user;
  bool get isLoading => _isLoading;
  bool get isAuthenticated => _user != null;
  String? get error => _error;

  final AuthService _authService = AuthService();
  
  // Sign in with email and password
  Future<bool> signInWithEmailAndPassword(String email, String password) async {
    _isLoading = true;
    _error = null;
    notifyListeners();
    
    try {
      final user = await _authService.signInWithEmailAndPassword(email, password);
      _user = user;
      _isLoading = false;
      notifyListeners();
      return true;
    } catch (e) {
      _error = e.toString();
      _isLoading = false;
      notifyListeners();
      return false;
    }
  }
  
  // Sign up with email and password
  Future<bool> signUpWithEmailAndPassword(String email, String password, String displayName) async {
    _isLoading = true;
    _error = null;
    notifyListeners();
    
    try {
      final user = await _authService.signUpWithEmailAndPassword(email, password, displayName);
      _user = user;
      _isLoading = false;
      notifyListeners();
      return true;
    } catch (e) {
      _error = e.toString();
      _isLoading = false;
      notifyListeners();
      return false;
    }
  }
  
  // Sign in with Google
  Future<bool> signInWithGoogle() async {
    _isLoading = true;
    _error = null;
    notifyListeners();
    
    try {
      final user = await _authService.signInWithGoogle();
      _user = user;
      _isLoading = false;
      notifyListeners();
      return true;
    } catch (e) {
      _error = e.toString();
      _isLoading = false;
      notifyListeners();
      return false;
    }
  }
  
  // Sign out
  Future<void> signOut() async {
    _isLoading = true;
    notifyListeners();
    
    try {
      await _authService.signOut();
      _user = null;
      _isLoading = false;
      notifyListeners();
    } catch (e) {
      _error = e.toString();
      _isLoading = false;
      notifyListeners();
    }
  }
  
  // Update user profile
  Future<bool> updateProfile({String? displayName, String? photoUrl, String? phoneNumber, String? address}) async {
    _isLoading = true;
    _error = null;
    notifyListeners();
    
    try {
      final updatedUser = await _authService.updateProfile(
        user: _user!,
        displayName: displayName,
        photoUrl: photoUrl,
        phoneNumber: phoneNumber,
        address: address,
      );
      
      _user = updatedUser;
      _isLoading = false;
      notifyListeners();
      return true;
    } catch (e) {
      _error = e.toString();
      _isLoading = false;
      notifyListeners();
      return false;
    }
  }
  
  // Reset password
  Future<bool> resetPassword(String email) async {
    _isLoading = true;
    _error = null;
    notifyListeners();
    
    try {
      await _authService.resetPassword(email);
      _isLoading = false;
      notifyListeners();
      return true;
    } catch (e) {
      _error = e.toString();
      _isLoading = false;
      notifyListeners();
      return false;
    }
  }
  
  // Check authentication state
  Future<void> checkAuthState() async {
    try {
      final user = await _authService.getCurrentUser();
      _user = user;
      notifyListeners();
    } catch (e) {
      _error = e.toString();
      notifyListeners();
    }
  }
}
```

For remaining sections, including other providers, screens, services, utils, and widgets, please refer to the additional documentation files in the docs directory.
